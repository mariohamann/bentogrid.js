{
  "name": "@bentogrid/root",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "generate": "pnpm generate-readme && pnpm generate-types",
    "generate-readme": "pnpm --filter @bentogrid/docs generate-readme",
    "generate-types": "pnpm --filter @bentogrid/core generate-types"
  }
}
